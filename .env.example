# Optional worker-only PostgreSQL URL. Leave unset for all localhost website features.
# Supply to the shell explicitly (the TypeScript worker does not automatically load .env).
# Remote connections require TLS; use a dedicated login granted asx_ingest after init.
# Never use NEXT_PUBLIC_ or commit the real value.
DATABASE_URL=
